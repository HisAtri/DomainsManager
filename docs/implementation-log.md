# 实施变更记录

## 2026-08-28 — M6 后台组件关联日志

- Worker、Scheduler 和 Notifier 对实际工作循环输出统一结构化完成/失败事件，包含组件实例 ID、处理数和耗时。
- 空轮询不产生日志；失败事件仅保留异常类型，不记录可能包含敏感数据的异常文本。

## 2026-08-28 — 文档与实际进度对账

- M4 已完成；M6 进入进行中状态，已交付的可观测性、安全审计、清理、独立 migration、运维手册和 PostgreSQL CI 能力均已回填到路线图。
- SMTP 密码按当前产品决策为管理员可明文持久化和读取的配置项；相关方案和 API 文档不再描述为加密且不回显。

## 2026-08-28 — M6 安全审计异常告警

- 新增带显式时间窗口的 `domainsmanager-audit-monitor`，仅聚合 Refresh Token 重放事件。
- 指标与告警事件只包含时间窗口和计数，不包含审计元数据、IP 哈希、请求 ID 或用户明细。

## 2026-08-28 — M6 管理端安全审计列表

- 管理员界面接入安全审计的事件、操作者和日期筛选以及分页列表。
- 界面只渲染已脱敏的标识和时间字段，不显示审计元数据、IP 哈希或请求 ID。

## 2026-08-28 — M6 安全审计查询

- 新增管理员只读审计查询，支持事件类型、操作者和时间范围筛选及稳定分页。
- 响应排除审计元数据、IP 哈希和请求 ID，避免把可能含敏感上下文的数据重新暴露给管理端。

## 2026-08-28 — M6 运行快照监控事件

- 新增 `domainsmanager-monitor`，输出结构化运行快照，并只在死信、过期租约或逾期监控非零时输出结构化告警事件。
- 管理员 API 与运维命令复用同一聚合逻辑；命令不执行迁移，也不包含域名、用户、凭据或错误原文。

## 2026-08-28 — M6 管理端运行状态面板

- 管理员界面接入运行快照，展示刷新任务、通知投递、过期租约、死信和逾期监控的聚合状态。
- 页面仅手动刷新，不以浏览器轮询替代部署侧持续监控；不展示域名、用户、凭据或错误原文。

## 2026-08-28 — M6 运行队列指标 API

- 新增管理员只读运行快照，聚合刷新任务、通知 Outbox、过期租约与到期未执行监控数量。
- 指标不返回域名、用户、凭据或错误原文；普通用户不能访问。
- 同步 OpenAPI 与可观测性文档，为后续告警和管理端状态面板提供共同数据源。

## 2026-08-28 — M6 备份与发布运维手册

- 新增备份恢复、独立 migration、滚动升级与回滚步骤，明确组件部署顺序和健康检查点。
- 回滚手册区分兼容 migration 与必须从备份恢复的情况，并明确记录清理不可撤销。

## 2026-08-28 — M6 独立 migration 入口

- 新增 `domainsmanager-migrate` 独立命令，默认升级数据库到 Alembic `head`。
- API lifespan 默认不再迁移数据库；本地兼容行为由显式 `DOMAINSMANAGER_MIGRATE_ON_STARTUP=true` 控制。
- 新增迁移运行手册，要求在应用启动前执行迁移、备份并验证发布兼容性。

## 2026-08-28 — M6 显式业务记录清理

- 新增独立 `domainsmanager-cleanup` 入口，对 Session、Refresh Token、幂等记录、终态任务/通知、审计和过期租约提供显式 cutoff 清理。
- 默认 dry-run，只有 `--apply` 执行删除；没有把未决的保留天数硬编码到应用配置。
- 清理范围排除域名、检查历史、规则、活跃任务、运行中投递和缓存/原始查询记录，并记录在数据保留手册中。

## 2026-08-28 — M6 认证接口基础限流

- 登录、注册、Token 刷新和预留密码重置路径按直接连接端地址与端点执行固定窗口限流，默认 60 秒 20 次。
- 超限响应使用稳定 `rate_limited` 错误、`Retry-After`、`no-store` 和 request ID；不同端点分别计数。
- 文档明确进程内计数器的边界：多实例生产部署仍需可信入口层的共享限流，应用不信任客户端提供的转发地址头。

## 2026-08-28 — M6 结构化访问日志基线

- request ID 中间件为每个 HTTP 请求写入紧凑 JSON 事件，包含请求 ID、方法、路径、状态码和处理耗时。
- 访问日志明确排除查询字符串、Header、Cookie、请求体和响应体，并以测试验证常见请求 Secret 不会进入日志。
- 新增可观测性文档，记录事件字段、敏感信息边界和后续指标/Tracing 范围。

## 2026-08-28 — M4 CI 门禁扩展

- CI 增加前端 `npm ci` 与生产构建，防止管理员页面的 TypeScript 或打包回归。
- Ruff 门禁扩展到已清理的 M4 管理员契约、API 测试、并发测试和索引迁移文件；其余历史文件继续分批整理。
- 新增 PostgreSQL 15 专项任务，在独立数据库中运行全部 `postgres` 标记测试，不再依赖默认测试任务中的跳过结果。

## 2026-08-28 — M4 静态检查基线整理

- 机械格式化管理员 API 测试和管理员索引迁移，不改变运行行为。
- 这部分目标文件现已通过 Ruff format 与 lint，可在后续 CI 子任务中直接纳入门禁。

## 2026-08-28 — M4 管理员检查记录前端闭环

- 新增独立的管理员检查记录页，支持按结果、错误码、时间范围、域名和用户筛选，并提供分页。
- 使用接口返回的全量匹配统计展示成功与非成功数量及结果分组，不以当前页数据冒充全局统计。
- 检查记录可打开关联域名详情，继续复用已有的编辑、软删除、强制刷新和历史查看流程。

## 2026-08-28 — M4 管理员域名前端闭环

- 全局域名页接入服务器端删除状态与排序参数，新增域名详情入口和所有者、状态、检查历史展示。
- 管理员可在详情中编辑监控、续期方式和备注，并处理版本冲突；软删除与强制刷新均使用后端要求的确认头或幂等键。
- 强制刷新后展示任务受理状态，并允许重新加载域名和检查记录观察最终结果。

## 2026-08-28 — M4 管理员用户前端闭环

- 用户管理页新增详情入口，支持编辑邮箱并查看域名数量、账号状态和最近登录时间。
- 用户详情展示安全化会话列表，并为有效会话提供带确认的单会话撤销操作；不展示 Token、完整 IP 或 User-Agent。
- 管理员用户列表固定使用后端用户名排序，保持刷新后的展示顺序稳定。

## 2026-08-27 — M4 管理列表契约补齐

- 管理员用户列表支持按创建时间、用户名和最近登录时间排序，并始终使用主键作为稳定分页的次级排序。
- 全局域名列表补齐公共后缀、监控状态、到期区间、最近结果筛选，以及创建时间、域名、到期时间和最近检查时间排序。
- 全局检查历史补齐检查时间区间筛选，并把检查结果限制到 OpenAPI 已声明的稳定枚举。
- 新增管理员默认域名列表、按所有者域名列表和全局检查时间线索引；SQLite 验证迁移打包，PostgreSQL 专项用例使用 `EXPLAIN` 验证查询计划。

## 2026-08-27 — M4 管理操作并发幂等

- 管理员封禁、解封和单会话撤销改用带状态前置条件的数据库更新；重复或并发请求只允许一个请求改变状态并写入审计。
- 解封显式恢复账户的 `is_active` 状态，但不会恢复封禁前已撤销的会话或 Refresh Token。
- SQLite API 回归覆盖重复请求；PostgreSQL 专项验收覆盖并发封禁、解封和会话撤销。

## 2026-08-27 — 站点信息与页面配置

- 扩展 `global_setting.value` 为长文本，增加站点名称、Logo、Favicon、页脚链接、版权、备案、自定义 CSS/JavaScript、HTML 注入及网站统计注册键。
- 新增公开只读的 `GET /api/v1/site/config`，支持默认值回退和 ETag；管理写入继续复用原子批量更新、版本冲突和审计链路。
- 重构系统设置编辑体验，增加资源预览、结构化页脚链接、代码输入、脏字段保存和保存后即时生效。
- 前端运行时接入 title、品牌 Logo、favicon、页脚及自定义页面内容；默认资源为 `/default.svg`。
- 新增 API 契约和回归测试，并完成浏览器实际交互验收。

本文档记录已合并的后端能力及其对 API、前端对接和运行配置的影响。功能实现必须在同一提交中更新本记录；HTTP 契约变更还必须同步更新 `docs/api/openapi.yaml`。

## 2026-08-26 — 当前状态对账与 M4 配置中心实现

- 对账确认 M1–M3 的实现已完成；M4 已实现管理员授权、用户管理、全局域名与检查记录管理、会话撤销、后台设置中心和运行期配置重载，阶段状态保持“进行中”。
- M4 的 SQLite/API/前端验收已完成；剩余发布门槛是专用 PostgreSQL 实例上的并发与查询计划验收，默认回归中的 PostgreSQL 用例会跳过。
- 新增 [全局运行配置管理](global-configuration-management.md) 方案。`global_setting` 承载经注册的业务策略；SMTP 密码是加密、仅写入且不回显的例外。数据库连接、其他部署 Secret、TLS、CORS 与首次管理员引导继续由部署环境管理。
- 最新离线回归为 `126 passed, 10 skipped`，并已验证前端生产构建；全仓 Ruff 格式检查仍有历史格式化债务，CI 的静态检查范围也尚未覆盖全部新增模块。

## 2026-08-24 — M2 刷新任务验收收尾

### 后端变更

- Worker 在 RDAP/WHOIS 查询期间每隔任务租约的三分之一续期一次；若租约已被其他 Worker 接管，当前 Worker 不再写入任务结果。
- 成功查询会为 JSON 快照生成稳定的 SHA-256 哈希，并与最近一次成功快照比较 `registrar`、`statuses`、`registered_at`、`expires_at`、`updated_at`、`nameservers` 和 `dnssec_enabled`。
- `DomainCheck.changed_fields` 仅包含上述发生变化的字段；首次成功检查保持为空数组。
- 成功查询后会写入 `ManagedDomain.next_check_at`。默认周期为 24 小时，配置项为 `DOMAINSMANAGER_CHECK_INTERVAL_SECONDS`。
- RFC 3339 快照时间在持久化边界转换为 UTC `datetime` 后再写入域名最新状态，避免把 JSON 字符串写入数据库时间列。

### API 与前端对接

- 现有 `GET /domains/{id}/checks` 和 `GET /domains/{id}/checks/{check_id}` 已返回 `changed_fields`，本次没有新增或修改 HTTP 字段，因此前端无需修改请求、轮询或类型定义。
- 前端可在检查历史中将非空 `changed_fields` 渲染为“本次变更字段”；字段为空代表首次成功检查或快照未变化。
- `next_check_at` 目前是后端调度预备数据，普通用户域名 API 暂不暴露；M3 Scheduler API 设计时再决定是否展示。

### 验证

- `tests/test_task_policy.py` 覆盖租约续期、防止第二 Worker 重领、快照差异和下次检查时间。
- 每次变更后运行目标测试、静态检查和全量测试；PostgreSQL 多 Worker 并发测试将在 M2 最终验收中补齐。

## 2026-08-24 — M3 Scheduler 基础链路

### 后端变更

- 新增独立的 `domainsmanager-scheduler` 进程。它扫描已启用、未软删除且 `next_check_at <= now` 的域名。
- Scheduler 使用数据库行锁和 `SKIP LOCKED` 领取域名；领取、推进 `next_check_at`、创建刷新任务及幂等记录处于同一事务中。
- 默认批量大小为 100，轮询间隔为 10 秒；可通过 `DOMAINSMANAGER_SCHEDULER_BATCH_SIZE` 和 `DOMAINSMANAGER_SCHEDULER_POLL_INTERVAL_SECONDS` 调整。
- Scheduler 创建的任务不跳过查询缓存。任务成功后继续由 Worker 写入下一次检查时间；任务重试或失败遵循既有 M2 策略。

### API 与前端对接

- 本提交未改变 HTTP API 或前端数据类型。Scheduler 是部署侧后台进程，前端继续通过现有任务轮询和检查历史接口观察结果。
- 后续通知规则和投递历史 API 将在 M3 Outbox 提交中记录在本文件和 `frontend/README.md`。

### 验证

- `tests/test_scheduler.py` 覆盖到期域名入队、监控关闭域名排除和重复扫描不重复建任务。

## 2026-08-24 — M3 通知规则基础 API

- 新增 `/api/v1/notification-rules` 的创建和列表接口，规则可绑定单一域名或作为用户全局规则。
- 支持 `expiration`、`status_change` 和 `query_failure` 事件，以及 `email`、`webhook` 渠道。到期规则必须指定提前天数。
- Webhook 规则当前只保存公开 URL；认证凭据、密钥和 Authorization 头不允许通过该接口或 JSON 配置提交。Outbox 与发送 Worker 将在下一提交实现。
- 前端尚未新增页面；可在后续通知设置页面调用上述接口。

## 2026-08-24 — M3 检查结果 Outbox

- 成功检查的状态变化、到期窗口，以及失败检查会在同一数据库事务中创建 `notification_outbox` 记录。
- Outbox 去重键由规则、检查或到期日组成；并发重复写入受唯一约束保护，不会回滚域名检查事务。
- 本次未改变浏览器 API；后续投递历史接口完成后再提供前端页面对接。

## 2026-08-24 — M3 Outbox 投递 Worker

- 新增 `domainsmanager-notifier` 独立进程，使用租约令牌领取 Outbox；投递成功标为 `sent`，失败重试，达到上限标为 `dead_letter`。
- Webhook 使用 HTTP POST；邮件使用用户账户邮箱和 SMTP 配置。SMTP 密码仅从环境变量读取，不写入规则、Outbox、日志或 API 响应。
- 前端可继续通过后续通知历史接口观察投递状态；本提交未新增浏览器端请求。

## 2026-08-24 — M3 通知重试与投递历史

- Outbox 投递失败后按 `base * 2^(attempt - 1)` 延迟重新入队，延迟受 `DOMAINSMANAGER_NOTIFICATION_RETRY_MAX_SECONDS` 限制；达到 `DOMAINSMANAGER_NOTIFICATION_MAX_ATTEMPTS` 后转入 `dead_letter`。
- 新增 `GET /api/v1/notification-rules/deliveries?limit=50`，仅返回当前用户规则产生的投递记录。响应不包含 Webhook URL、SMTP 配置、收件人或事件原始载荷。
- 保存的失败原因仅为异常类型和固定说明，不保留可能含有 URL、认证信息或服务端细节的原始异常文本。
- 前端可将 `status`、`attempt_count`、`available_at`、`sent_at` 和 `failure_reason` 显示在通知设置页的投递历史区域；本仓库当前没有对应前端页面，因此未产生前端源代码改动。

## 2026-08-24 — M2/M3 PostgreSQL 并发验收

- 新增显式 `postgres` 标记测试，验证两个刷新 Worker 只会领取同一任务一次、租约到期后可恢复领取，以及两个 Scheduler 不会为同一到期域名重复建任务。
- 测试仅在 `DOMAINSMANAGER_RUN_POSTGRES_TESTS=1` 且配置专用 PostgreSQL 测试库时执行；默认测试不会访问或清理 PostgreSQL。
- 为 Outbox 状态和重试次数添加数据库约束，阻止非法状态或负数尝试次数绕过应用状态机写入。
- 本地全量回归已通过；阶段状态仍保持“进行中”，直到在明确指定、可清空的 PostgreSQL 测试库中实际执行上述并发用例。

## 2026-08-24 — M2/M3 最终验收完成

- 在项目专用的临时 PostgreSQL 实例中，从无表结构数据库完成迁移冷启动、历史 revision 升级与回退后再升级验证。
- PostgreSQL 迁移与并发验收共 5 项通过；完整测试矩阵共 `123 passed`。刷新任务、Scheduler、Outbox 与通知 API 的 M2/M3 完成条件已满足。
- 同时修复 PostgreSQL 测试 schema 白名单和 head revision 断言，使新任务表、幂等记录和最新 Outbox 迁移在冷启动验收中受到持续覆盖。
