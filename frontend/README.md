# DomainsManager Web

基于 React、TypeScript、Tailwind CSS v4 和 Radix UI 的前端应用。它通过 Vite 代理调用后端的 `/api/v1` 接口，不使用演示数据。

## 启动

先启动后端（默认 `127.0.0.1:7920`），再执行：

```powershell
cd frontend
npm install
npm run dev
```

开发服务器运行在 `http://127.0.0.1:5173`；`vite.config.ts` 将 `/api` 请求代理至后端。

## 已对接的接口

- 本地登录、注册、Token 刷新、退出登录
- 当前用户资料、密码、监控偏好
- 域名列表、创建、详情、更新、软删除
- 域名刷新任务、任务轮询、检查历史
- 管理员用户查询、详情、邮箱编辑、封禁、解封和会话撤销
- 管理员全局域名查询、筛选、详情、编辑、软删除和强制刷新
- 管理员全局检查历史筛选、结果统计和关联域名详情

## 后端变更对接记录

- 2026-08-28：新增管理员检查记录页，接入结果、错误码、时间、域名和用户筛选，展示完整筛选集的结果分组统计，并可从检查记录进入关联域名详情。
- 2026-08-28：管理员用户列表已接入详情抽屉、邮箱编辑、会话概览和单会话撤销；会话只展示后端提供的安全时间与状态字段，不展示 Token、完整 IP 或 User-Agent。
- 2026-08-28：全局域名页已接入服务器端删除状态与排序参数，并提供详情编辑、检查历史、软删除确认和强制刷新任务反馈。

- 2026-08-24：检查历史接口的 `changed_fields` 现在会在成功刷新后的快照发生变化时返回字段名；现有 `Check` 类型无需调整。界面可将非空数组显示为本次域名信息变更。
- `next_check_at` 已由后端维护，暂不出现在普通用户域名响应中；后续 Scheduler 功能开放时会在本文件和 OpenAPI 契约中记录前端改动。
- 2026-08-24：后端 Scheduler 已独立部署并按 `next_check_at` 创建刷新任务；没有新增浏览器端接口或页面改动，现有任务轮询仍是观察刷新进度的方式。
- 2026-08-24：已补充 `NotificationRule` 和 `NotificationDelivery` 前端类型，以及通知规则列表/创建、投递历史读取方法。当前尚未新增通知设置页面；页面实现应只展示投递状态、尝试次数、时间和安全化失败原因，不能渲染或存储 Webhook URL 以外的通道凭据。

刷新任务中心仅展示当前浏览器会话创建的任务，因为后端目前没有任务列表接口。
